{
  "scripts": {
    "ui-dev": "cd ./ui && yarn start",
    "api-dev": "cd ./api && ./node_modules/nodemon/bin/nodemon.js",
    "db-dev": "docker-compose  -f 'docker-compose.yaml' up -d --build db"
  }
}
